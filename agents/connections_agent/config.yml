agent:
  name: connections_agent
  runtime: local_json
paths:
  data_dir: ./data
  runtime_dir: ./runtime
graph:
  nodes:
    - memory_retrieve
    - react
    - tool_execution
    - reflect
    - response
  max_steps: 6
