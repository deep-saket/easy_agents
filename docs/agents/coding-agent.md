# Coding Agent

Status: placeholder; not runnable.

This name reserves a future coding-focused agent built on the shared platform. The current package contains only `agents/coding_agent/__init__.py`; it has no runtime class, graph, tools, configuration, CLI, sandbox policy, or tests.

A future coding agent will need an especially strict capability boundary for shell commands, filesystem writes, repository state, approvals, and audit logs. Use [Create an Agent](../guides/create-an-agent.md) for the current code-first workflow and the [Offline Agent Platform Roadmap](../plans/offline-agent-platform-roadmap.md) for the planned platform controls.
